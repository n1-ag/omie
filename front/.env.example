# Strapi CMS — URL da API sem barra no final (local ou Strapi Cloud)
STRAPI_API_URL=http://localhost:1337

# Token de API (criar em Strapi: Settings → API Tokens)
STRAPI_API_TOKEN=

# true = usa dados mock (sem Strapi); false = usa a API acima
STRAPI_MOCK=false

# URL pública do site (canonical, Open Graph, structured data)
NEXT_PUBLIC_SITE_URL=https://www.omie.com.br
